# this config doesn't have SNI a presumed present field
#sni: false
port: 1965
hostname: localhost

hosts {
  localhost {
    docs {
      root: /var/gemini
    }
    cert {
      file: cert.pem
      key: key.pem
    }
  }
}
